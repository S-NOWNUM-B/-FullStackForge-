# Структура исходного кода

Организация файлов следует лучшим практикам Next.js и принципам чистой архитектуры.

## 📁 Структура папок

```
src/
├── app/                    # Next.js App Router
│   ├── (page)/            # Публичные страницы (главная, About, Projects, Work)
│   ├── admin/             # Админ-панель с защищенным доступом
│   └── api/               # API routes (REST endpoints)
│
├── components/            # React компоненты
│   ├── providers/         # Context провайдеры (Theme, etc.)
│   └── ui/                # UI компоненты (Button, Card, Input, etc.)
│
├── config/                # Конфигурационные файлы
│   ├── auth.config.ts     # Конфигурация NextAuth.js
│   └── auth.ts            # Провайдеры и настройки аутентификации
│
├── constants/             # Константы приложения
│   ├── technologies.ts    # Список технологий для About страницы
│   └── project-technologies.ts  # Технологии для проектов
│
├── services/              # Внешние сервисы и API клиенты
│   └── firebase.ts        # Firebase Admin SDK и Firestore
│
├── types/                 # TypeScript определения типов
│   ├── api.ts             # Типы для API responses
│   ├── env.d.ts           # Типы переменных окружения
│   └── next-auth.d.ts     # Расширение типов NextAuth
│
├── utils/                 # Вспомогательные функции
│   └── cn.ts              # Утилита для объединения classNames
│
├── hooks/                 # Custom React хуки (пусто пока)
│
└── middleware.ts          # Next.js middleware для защиты роутов
```

## 🎯 Принципы организации

### 1. **Разделение по назначению**
- `app/` - логика роутинга и страницы
- `components/` - переиспользуемые UI компоненты
- `services/` - интеграции с внешними сервисами
- `utils/` - чистые функции-утилиты
- `config/` - конфигурация приложения

### 2. **Чистый импорт**
Используются алиасы для удобных импортов:
```typescript
import { db } from '@/services/firebase';
import { Button } from '@/components/ui/Button';
import { TECHNOLOGIES } from '@/constants/technologies';
```

### 3. **Типизация**
Все типы собраны в `types/`, что упрощает их переиспользование и поддержку.

### 4. **Масштабируемость**
Структура готова к росту проекта:
- Добавляйте новые хуки в `hooks/`
- Создавайте новые сервисы в `services/`
- Расширяйте константы в `constants/`

## 📝 Соглашения

- Компоненты используют PascalCase: `Button.tsx`
- Утилиты используют camelCase: `cn.ts`
- Константы используют UPPER_SNAKE_CASE внутри файлов
- Один компонент = один файл
