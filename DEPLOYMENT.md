# 🚀 Инструкция по развертыванию на Render

## ✅ Исправленные ошибки

Все ошибки сборки исправлены:
- ✅ Создан файл `not-found.tsx` для обработки 404 ошибок
- ✅ Исправлены предупреждения ESLint с useEffect зависимостями
- ✅ Устранен дублирующийся индекс в модели Config
- ✅ Проект успешно собирается локально

## 🔧 Настройки для Render

### 1. Переменные окружения
```
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/portfolio
NODE_ENV=production
ADMIN_PASSWORD=ваш_безопасный_пароль
```

### 2. Команды сборки и запуска
- **Build Command:** `pnpm install --frozen-lockfile && pnpm build`
- **Start Command:** `pnpm start`
- **Node Version:** 18.x или выше

### 3. Health Check
- **Path:** `/healthz` (стандартный путь)

## 📋 Пошаговая инструкция

1. **Загрузите код в GitHub** (если еще не сделано)
2. **Создайте MongoDB Atlas кластер** (бесплатный)
3. **На Render:**
   - Подключите GitHub репозиторий
   - Выберите "New Web Service"
   - Настройте переменные окружения
   - Запустите развертывание

## 🎯 Результат

После успешного развертывания ваш сайт будет доступен по адресу:
- **Основной сайт:** `https://your-app-name.onrender.com`
- **Админ-панель:** `https://your-app-name.onrender.com/admin`

## ⚠️ Важные моменты

- Админ-панель защищена паролем из переменной `ADMIN_PASSWORD`
- Изображения сохраняются как base64 в MongoDB
- Email настройки опциональны (для контактной формы)
- Render предоставляет бесплатный SSL сертификат
