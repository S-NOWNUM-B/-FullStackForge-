# Настройка переменных окружения на Render.com

## 🚨 Важно: Исправление ошибок 503

Если вы видите ошибки 503 на странице проектов, значит не настроены переменные окружения Firebase на Render.

## 📝 Пошаговая инструкция

### 1. Откройте Render Dashboard

1. Перейдите на [dashboard.render.com](https://dashboard.render.com)
2. Выберите ваш сервис (portfolio-website)
3. Перейдите в раздел **Environment**

### 2. Добавьте Firebase переменные

#### Получите Firebase credentials:

1. Откройте [Firebase Console](https://console.firebase.google.com)
2. Выберите проект **fullstackforge-6618e**
3. Перейдите в **Project Settings** (⚙️) → **Service Accounts**
4. Нажмите **Generate New Private Key**
5. Скачается JSON файл с credentials

#### Добавьте переменные в Render:

**FIREBASE_PROJECT_ID**
```
fullstackforge-6618e
```

**FIREBASE_CLIENT_EMAIL**
```
firebase-adminsdk-xxxxx@fullstackforge-6618e.iam.gserviceaccount.com
```
(Скопируйте из JSON файла поле `client_email`)

**FIREBASE_PRIVATE_KEY**
```
-----BEGIN PRIVATE KEY-----
[Ваш приватный ключ]
-----END PRIVATE KEY-----
```
(Скопируйте из JSON файла поле `private_key` целиком, включая `-----BEGIN` и `-----END`)

⚠️ **ВАЖНО**: Приватный ключ должен содержать символы `\n` для переносов строк. Render автоматически обработает их.

### 3. Добавьте Authentication переменные

**ADMIN_USERNAME**
```
admin
```
(или ваш логин)

**ADMIN_PASSWORD_HASH**
```bash
# Сгенерируйте хеш локально:
node -e "const bcrypt=require('bcrypt');bcrypt.hash('ваш_пароль',10).then(console.log)"
# Скопируйте результат в переменную
```

**AUTH_SECRET**
```bash
# Сгенерируйте secret:
openssl rand -base64 32
# Скопируйте результат в переменную
```

**NEXTAUTH_URL**
```
https://fullstackforge.onrender.com
```

### 4. Добавьте Email переменные (опционально)

**RESEND_API_KEY**
```
re_xxxxxxxxxxxxxxxxx
```
(Получите на [resend.com](https://resend.com))

**EMAIL_FROM**
```
noreply@yourdomain.com
```

**EMAIL_TO**
```
your@email.com
```

### 5. Добавьте Cron переменную

**CRON_SECRET**
```bash
# Сгенерируйте случайную строку:
openssl rand -hex 32
```

### 6. Перезапустите сервис

После добавления всех переменных:

1. Нажмите **Save Changes**
2. Render автоматически перезапустит приложение
3. Дождитесь завершения деплоя (2-3 минуты)

### 7. Проверьте работу

1. Откройте сайт: https://fullstackforge.onrender.com
2. Перейдите на страницу **Проекты**
3. Проекты должны загружаться без ошибок 503

## 🔍 Проверка переменных

Вы можете проверить статус Firebase через API:

```bash
curl https://fullstackforge.onrender.com/api/health
```

Должен вернуть:
```json
{
  "status": "healthy",
  "firebase": "connected",
  "timestamp": "..."
}
```

## 📋 Полный список обязательных переменных

✅ **Firebase** (обязательно):
- `FIREBASE_PROJECT_ID`
- `FIREBASE_CLIENT_EMAIL`
- `FIREBASE_PRIVATE_KEY`

✅ **Authentication** (обязательно):
- `ADMIN_USERNAME`
- `ADMIN_PASSWORD_HASH`
- `AUTH_SECRET`
- `NEXTAUTH_URL`

⚠️ **Email** (опционально, для формы контактов):
- `RESEND_API_KEY`
- `EMAIL_FROM`
- `EMAIL_TO`

⚠️ **Cron** (опционально, для scheduled tasks):
- `CRON_SECRET`

## 🐛 Troubleshooting

### Проблема: Все еще ошибка 503

**Решение:**
1. Проверьте что все Firebase переменные добавлены
2. Убедитесь что `FIREBASE_PRIVATE_KEY` содержит полный ключ с `-----BEGIN` и `-----END`
3. Проверьте логи в Render: **Logs** → ищите "Firebase Admin инициализирован"

### Проблема: "Firebase credentials не настроены"

**Решение:**
1. Переменные содержат placeholder значения (например, "your-project-id")
2. Удалите старые значения и добавьте реальные из Firebase Console

### Проблема: "Error: Cannot parse" при инициализации

**Решение:**
1. Проблема с форматом `FIREBASE_PRIVATE_KEY`
2. Убедитесь что ключ скопирован полностью
3. Проверьте что нет лишних пробелов в начале/конце

## 📞 Поддержка

Если проблемы остались:
1. Проверьте логи Render
2. Проверьте Firebase Console → Service Accounts → статус ключа
3. Попробуйте сгенерировать новый Private Key

---

**Важно**: После настройки переменных сайт будет работать корректно. Firebase бесплатен для вашего объема данных.
