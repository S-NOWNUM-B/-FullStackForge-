# Структура проекта FullStackForge

Профессиональная организация Next.js проекта с TypeScript, Firebase и NextAuth.

## 🏗️ Корневая структура

```
fullStackForge/
├── .next/                 # Build артефакты Next.js (игнорируется)
├── .firebase/             # Firebase локальные файлы (игнорируется)
├── node_modules/          # Зависимости (игнорируется)
│
├── public/                # Статические файлы (доступны по /)
│   ├── profile.jpeg       # Фото профиля
│   └── resume.pdf         # Резюме
│
├── src/                   # Исходный код приложения
│   └── [см. src/README.md]
│
├── docs/                  # Документация проекта
│   └── STRUCTURE.md       # Этот файл
│
├── .env.local             # Переменные окружения (НЕ коммитится)
├── .env.example           # Пример переменных окружения
├── .gitignore             # Игнорируемые файлы Git
│
├── eslint.config.mjs      # Конфигурация ESLint
├── next.config.ts         # Конфигурация Next.js
├── next-env.d.ts          # TypeScript определения Next.js
├── postcss.config.mjs     # Конфигурация PostCSS
├── tailwind.config.ts     # Конфигурация Tailwind CSS
├── tsconfig.json          # Конфигурация TypeScript
│
├── package.json           # Зависимости и скрипты
├── pnpm-lock.yaml         # Lock-файл pnpm
│
├── render.yaml            # Конфигурация Render.com
└── README.md              # Главная документация
```

## 🔐 Переменные окружения

Создайте `.env.local` на основе `.env.example`:

```bash
# Firebase Admin SDK
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=

# Admin credentials
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=

# Email (Resend)
RESEND_API_KEY=
EMAIL_FROM=
EMAIL_TO=
```

## 📦 Зависимости

### Основные
- **Next.js 16** - React фреймворк
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация

### Аутентификация
- **NextAuth.js v5** - Аутентификация
- **bcrypt** - Хеширование паролей

### База данных
- **Firebase Admin** - Firestore серверный SDK
- **Firebase** - Клиентский SDK

### UI компоненты
- **Radix UI** - Unstyled компоненты
- **Framer Motion** - Анимации
- **Lucide React** - Иконки

### Email
- **Resend** - Отправка email

## 🚀 Скрипты

```bash
# Разработка
pnpm dev           # Запуск dev сервера (http://localhost:3000)

# Production
pnpm build         # Сборка для продакшена
pnpm start         # Запуск production сервера

# Линтинг
pnpm lint          # Проверка кода ESLint
```

## 🌐 Deployment

Проект размещен на **Render.com**:
- Автоматический деплой из main ветки
- Node.js 22+ environment
- Все env переменные настроены в Render Dashboard

## 📚 Дополнительная документация

- [Структура src/](../src/README.md) - детальное описание исходного кода
- [README.md](../README.md) - главная страница проекта

## 🔄 Git workflow

```bash
git status                 # Проверить изменения
git add .                  # Добавить все файлы
git commit -m "message"    # Закоммитить
git push origin main       # Отправить в GitHub
```

Автоматический деплой на Render после push в main.
