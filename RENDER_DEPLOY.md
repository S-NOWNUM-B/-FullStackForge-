# 🚀 Деплой на Render

Пошаговая инструкция по развертыванию сайта-портфолио на Render.com (бесплатный тариф).

---

## 📋 Предварительные требования

- [x] Аккаунт на [Render.com](https://render.com)
- [x] Репозиторий GitHub с проектом
- [x] MongoDB Atlas (бесплатный кластер)
- [x] (Опционально) Gmail или другой SMTP для email

---

## 🎯 Быстрый старт

### 1. Подготовка MongoDB Atlas

1. Создайте бесплатный аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте новый кластер (FREE tier)
3. Создайте пользователя базы данных:
   - Username: `portfolio_user`
   - Password: *сгенерируйте надежный пароль*
4. Добавьте IP адрес:
   - Network Access → Add IP Address → Allow Access from Anywhere (`0.0.0.0/0`)
5. Получите Connection String:
   - Connect → Connect your application → Copy connection string
   - Замените `<password>` на ваш пароль
   - Пример: `mongodb+srv://portfolio_user:PASSWORD@cluster0.xxxxx.mongodb.net/portfolio?retryWrites=true&w=majority`

### 2. Создание Web Service на Render

1. Войдите на [Render Dashboard](https://dashboard.render.com/)
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий: `S-NOWNUM-B/-BUISNES-CARD-`

### 3. Настройка Web Service

#### Основные настройки:

- **Name:** `buisnes-card` (или любое имя)
- **Region:** Frankfurt (Europe) - ближайший к Казахстану
- **Branch:** `main`
- **Root Directory:** (оставьте пустым)
- **Runtime:** Node
- **Build Command:** `pnpm install --frozen-lockfile && pnpm run build`
- **Start Command:** `pnpm start`
- **Plan:** Free

#### Environment Variables (переменные окружения):

Нажмите **Advanced** и добавьте следующие переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `NODE_VERSION` | `20` | Версия Node.js |
| `NODE_ENV` | `production` | Окружение |
| `MONGODB_URI` | `mongodb+srv://...` | Ваш MongoDB connection string |
| `ADMIN_PASSWORD` | `ваш_пароль` | Пароль для админ-панели |
| `SMTP_HOST` | `smtp.gmail.com` | (Опционально) SMTP сервер |
| `SMTP_PORT` | `587` | (Опционально) SMTP порт |
| `SMTP_USER` | `your@gmail.com` | (Опционально) Email |
| `SMTP_PASSWORD` | `app_password` | (Опционально) App Password |
| `SMTP_FROM` | `your@gmail.com` | (Опционально) От кого |

### 4. Запуск деплоя

1. Нажмите **Create Web Service**
2. Render начнет сборку (займет 5-10 минут)
3. После успешной сборки ваш сайт будет доступен по адресу: `https://buisnes-card.onrender.com`

---

## ⚙️ Альтернативный способ (через render.yaml)

Если в репозитории есть `render.yaml`, Render автоматически применит настройки:

1. Убедитесь что `render.yaml` есть в корне проекта
2. Коммитьте и пушьте изменения
3. Render автоматически создаст сервис с настройками из файла
4. Добавьте переменные окружения вручную в Dashboard

---

## 🔧 Настройка переменных окружения

### Обязательные переменные:

```bash
# MongoDB (обязательно)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/portfolio

# Admin панель (обязательно)
ADMIN_PASSWORD=your_secure_admin_password
```

### Опциональные переменные (для email):

```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_FROM=your_email@gmail.com
```

---

## 🎨 Проверка деплоя

После успешного деплоя проверьте:

1. ✅ **Главная страница:** `https://your-app.onrender.com`
2. ✅ **Страница About:** `https://your-app.onrender.com/About`
3. ✅ **Проекты:** `https://your-app.onrender.com/Projects`
4. ✅ **Контакты:** `https://your-app.onrender.com/Contacts`
5. ✅ **Админ-панель:** `https://your-app.onrender.com/admin`

### Проверка API:

```bash
# Получить все проекты
curl https://your-app.onrender.com/api/projects

# Получить фильтры
curl https://your-app.onrender.com/api/projects/filters
```

---

## 📊 Мониторинг и логи

### Просмотр логов:

1. Откройте ваш сервис в Render Dashboard
2. Перейдите во вкладку **Logs**
3. Здесь вы увидите:
   - Логи сборки
   - Логи выполнения
   - Ошибки (если есть)

### Полезные логи:

```
✅ MongoDB подключен успешно
📧 Новый запрос: {...}
✅ Email успешно отправлен
```

---

## 🔄 Автоматический деплой

Render автоматически перезапускает деплой при:

- Push в ветку `main`
- Изменении переменных окружения (если включена автоматическая пересборка)

### Ручной деплой:

1. Render Dashboard → Ваш сервис
2. Нажмите **Manual Deploy** → **Deploy latest commit**

---

## ⚡ Оптимизация для бесплатного плана

### Особенности бесплатного плана:

- ⏰ **Spin down после 15 минут неактивности**
- 🕐 **Первый запрос после spin down занимает ~30 секунд**
- 💾 **750 часов в месяц (достаточно для одного сервиса)**

### Как разбудить сервис:

1. Используйте **UptimeRobot** (бесплатно):
   - Создайте HTTP(s) монитор
   - URL: `https://your-app.onrender.com`
   - Interval: 5 минут
   
2. Или создайте cron job:
   ```bash
   */5 * * * * curl https://your-app.onrender.com
   ```

---

## 🐛 Troubleshooting

### Проблема: Build failed

**Решение:**
```bash
# Проверьте логи в Render Dashboard
# Убедитесь что:
1. package.json содержит правильные скрипты
2. Нет --turbopack флага в build команде
3. next.config.ts не содержит output: 'standalone'
```

### Проблема: MongoDB connection failed

**Решение:**
```bash
# Проверьте:
1. MONGODB_URI правильно настроен
2. IP адрес 0.0.0.0/0 добавлен в MongoDB Atlas
3. Пользователь БД создан с правильными правами
```

### Проблема: 404 на всех страницах

**Решение:**
```bash
# Убедитесь что:
1. Build прошел успешно
2. .next папка создана
3. Start команда: pnpm start (не pnpm dev)
```

### Проблема: Медленный первый запрос

**Причина:** Сервис спит после 15 минут неактивности (бесплатный план)

**Решение:**
- Используйте UptimeRobot для периодических пингов
- Или перейдите на платный план ($7/месяц)

---

## 🔐 Безопасность

### ✅ Что нужно делать:

- Используйте надежные пароли для `ADMIN_PASSWORD`
- Храните `MONGODB_URI` в переменных окружения (не в коде)
- Регулярно обновляйте зависимости: `pnpm update`
- Используйте HTTPS (Render предоставляет бесплатно)

### ❌ Что НЕ нужно делать:

- Не коммитьте `.env` файлы
- Не используйте простые пароли
- Не открывайте MongoDB для всех IP (если возможно ограничить)

---

## 📈 Переход на платный план

Когда нужен платный план ($7/месяц):

- ❌ Сервис не должен засыпать
- ✅ Нужны dedicated ресурсы
- ✅ Больше RAM/CPU
- ✅ Больше 750 часов в месяц

---

## 🌐 Кастомный домен

Для подключения своего домена:

1. Render Dashboard → Ваш сервис → Settings → Custom Domain
2. Добавьте ваш домен
3. Настройте DNS записи у вашего регистратора:
   ```
   CNAME: www → your-app.onrender.com
   A: @ → [IP от Render]
   ```

---

## 📚 Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [Render Node.js Deploy Guide](https://render.com/docs/deploy-node-express-app)
- [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

---

## ✅ Чек-лист перед деплоем

- [ ] MongoDB Atlas настроен и доступен
- [ ] Все переменные окружения добавлены в Render
- [ ] `next.config.ts` не содержит `output: 'standalone'`
- [ ] `package.json` build без `--turbopack`
- [ ] Код запушен в GitHub
- [ ] Логи проверены на ошибки
- [ ] Сайт доступен и работает

---

**Готово! Ваш сайт развернут на Render! 🎉**

Адрес: `https://your-app.onrender.com`
