# Настройка Cron для предотвращения засыпания сервиса

## ✅ Решение 1: Render Cron Jobs (Рекомендуется)

Уже настроено в `render.yaml`! После деплоя:

1. Зайдите в [Render Dashboard](https://dashboard.render.com)
2. Найдите ваш проект
3. Убедитесь что Cron Job `portfolio-keepalive` создан
4. Он будет автоматически пинговать ваш сайт каждые 14 минут

**Преимущества:**
- ✅ Бесплатно
- ✅ Встроено в Render
- ✅ Надежно
- ✅ Не требует внешних сервисов

---

## ✅ Решение 2: UptimeRobot (Дополнительно)

Бесплатный мониторинг с пингами каждые 5 минут:

### Настройка:

1. Зарегистрируйтесь на [UptimeRobot](https://uptimerobot.com)
2. Создайте новый Monitor:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: FullStackForge Keepalive
   - **URL**: `https://fullstackforge.onrender.com/api/health`
   - **Monitoring Interval**: 5 minutes
   - **Monitor Timeout**: 30 seconds

3. (Опционально) Добавьте Custom HTTP Headers:
   ```
   Authorization: Bearer YOUR_CRON_SECRET
   ```

**Преимущества:**
- ✅ Бесплатно до 50 мониторов
- ✅ Проверка каждые 5 минут
- ✅ Email уведомления при даунтайме
- ✅ Статистика uptime

---

## ✅ Решение 3: GitHub Actions (Альтернатива)

Создайте файл `.github/workflows/keepalive.yml`:

```yaml
name: Keepalive Ping

on:
  schedule:
    - cron: '*/10 * * * *'  # Каждые 10 минут
  workflow_dispatch:  # Ручной запуск

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health endpoint
        run: |
          curl -f https://fullstackforge.onrender.com/api/health || exit 0
```

**Преимущества:**
- ✅ Бесплатно
- ✅ Интегрировано с GitHub
- ✅ Гибкая настройка

---

## 🎯 Рекомендация

Используйте **Render Cron Jobs** (Решение 1) - уже настроено!

Для максимальной надежности добавьте **UptimeRobot** (Решение 2) - займет 2 минуты.

---

## 🔧 Проверка работы

После настройки проверьте:

```bash
# Проверьте что эндпоинт доступен
curl https://fullstackforge.onrender.com/api/health

# С авторизацией
curl -H "Authorization: Bearer YOUR_CRON_SECRET" \
     https://fullstackforge.onrender.com/api/cron
```

Должны получить:
```json
{
  "success": true,
  "message": "Ping successful",
  "timestamp": "2026-02-02T11:29:00.000Z"
}
```

---

## 📊 Мониторинг

Логи Render Cron Job можно посмотреть в Dashboard → Cron Job → Logs

Если используете UptimeRobot - статистика будет в их дашборде.
